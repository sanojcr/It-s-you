import { ReplaceCharactersPipe } from './replace-characters.pipe';

describe('ReplaceCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
